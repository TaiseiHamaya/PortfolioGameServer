pub fn run() {
    // 初期化

    loop {
        // proxy受信

        // proxy内にプログラム終了処理がある場合にbreak
        if false {
            break;
        }

        // 更新

        // proxy送信
    }

    // 終了処理
}
